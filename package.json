{
  "name": "scientific-calculator",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root for the scientific calculator monorepo (UI-agnostic core + web consumer).",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "build:core": "tsc -b packages/core",
    "build:web": "tsc -b packages/web"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
